import {notFound} from 'next/navigation';import {getDraftForSurvey} from '@/lib/data';import {QuestionBuilder} from '@/components/admin/QuestionBuilder';export default async function Questions({params}:{params:Promise<{id:string}>}){const {id}=await params;const draft=await getDraftForSurvey(id);if(!draft)notFound();return <QuestionBuilder surveyId={id} versionId={draft.id} initial={draft.questions}/>}
