import {notFound} from 'next/navigation';import {getDraftForSurvey,getSurvey} from '@/lib/data';import {SurveyRenderer} from '@/components/survey/SurveyRenderer';export default async function Preview({params}:{params:Promise<{id:string}>}){const {id}=await params;const [s,v]=await Promise.all([getSurvey(id),getDraftForSurvey(id)]);if(!s||!v)notFound();return <div><h1>下書きプレビュー</h1><p className="notice">公開中の画面ではなく、現在の下書きを表示しています。</p><div className="card" style={{padding:12,background:'#dbe2ea'}}><SurveyRenderer name={s.name} slug={s.slug} version={v} preview/></div></div>}
