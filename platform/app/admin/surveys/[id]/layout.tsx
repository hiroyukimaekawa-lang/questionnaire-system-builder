import Link from 'next/link'; export default async function SurveyLayout({children,params}:{children:React.ReactNode,params:Promise<{id:string}>}){const {id}=await params;return <div className="stack"><nav className="tabs" aria-label="アンケート編集"><Link href={`/admin/surveys/${id}`}>基本・デザイン</Link><Link href={`/admin/surveys/${id}/questions`}>質問</Link><Link href={`/admin/surveys/${id}/preview`}>プレビュー</Link><Link href={`/admin/surveys/${id}/responses`}>回答</Link><Link href={`/admin/surveys/new?duplicate=${id}`}>チャットで再設定</Link></nav>{children}</div>}
