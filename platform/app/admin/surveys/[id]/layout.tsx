import Link from 'next/link';import {notFound} from 'next/navigation';import {getUser} from '@/lib/data';export default async function SurveyLayout({children,params}:{children:React.ReactNode,params:Promise<{id:string}>}){const [{id},user]=await Promise.all([params,getUser()]);if(user?.role==='viewer')notFound();return <div className="stack"><nav className="tabs" aria-label="アンケート編集"><Link href={`/admin/manage/${id}`}>← 概要へ戻る</Link><Link href={`/admin/surveys/${id}`}>基本・デザイン</Link><Link href={`/admin/surveys/${id}/questions`}>質問</Link><Link href={`/admin/surveys/${id}/preview`}>プレビュー</Link><Link href={`/admin/manage/${id}/responses`}>回答</Link><Link href={`/admin/surveys/new?duplicate=${id}`}>チャットで再設定</Link></nav>{children}</div>}
