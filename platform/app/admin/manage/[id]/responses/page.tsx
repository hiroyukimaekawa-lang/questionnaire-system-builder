import Link from 'next/link';import {notFound} from 'next/navigation';import {getSurvey} from '@/lib/data';import {displayAnswer,responseData} from '@/lib/responses';
export default async function Responses({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{page?:string}>}){const [{id},query]=await Promise.all([params,searchParams]),page=Math.max(1,Number(query.page)||1),survey=await getSurvey(id);if(!survey)notFound();const {responses,questions,count,pageSize}=await responseData(id,page);const ordered=[...questions].sort((a:any,b:any)=>a.sort_order-b.sort_order),pages=Math.max(1,Math.ceil(count/pageSize));return <section className="stack"><div className="list-heading"><div><h2>回答一覧</h2><p>{count}件の回答</p></div><a className="btn secondary" href={`/api/admin/surveys/${id}/responses.csv`}>CSVダウンロード</a></div><div className="card response-table-wrap"><table><thead><tr><th>回答日時</th>{ordered.map((q:any)=><th key={q.id}>{q.title}</th>)}<th>合計点</th><th>平均点</th></tr></thead><tbody>{responses.map((r:any)=><tr key={r.id}><td>{new Date(r.submitted_at).toLocaleString('ja-JP',{timeZone:'Asia/Tokyo'})}</td>{ordered.map((q:any)=>{const answer=r.response_answers.find((item:any)=>item.question_id===q.id);return <td key={q.id}>{displayAnswer(answer,q)||'—'}</td>})}<td>{r.total_score??'—'}</td><td>{r.average_score??'—'}</td></tr>)}</tbody></table>{responses.length===0?<p className="empty-inline">回答はまだありません。</p>:null}</div>{pages>1?<nav className="pagination" aria-label="回答ページ送り"><Link aria-disabled={page<=1} href={page<=1?'#':`?page=${page-1}`}>前へ</Link><span>{page} / {pages}</span><Link aria-disabled={page>=pages} href={page>=pages?'#':`?page=${page+1}`}>次へ</Link></nav>:null}</section>}
