import { LoginForm } from '@/components/admin/LoginForm'; import { isSupabaseConfigured } from '@/lib/env';
export default function Login(){return <main className="shell login-shell"><section className="card stack login-card"><div><p className="muted">アンケートシステム</p><h1>管理画面ログイン</h1></div>{!isSupabaseConfigured&&<p className="error">Supabase環境変数を設定してください。</p>}<LoginForm disabled={!isSupabaseConfigured}/></section></main>}
