import {NextResponse} from 'next/server';import {getUser} from '@/lib/data';import {csvEscape} from '@/lib/survey';import {displayAnswer,responseData} from '@/lib/responses';
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){if(!await getUser())return NextResponse.json({error:'権限がありません。'},{status:401});const {id}=await params;const {responses,questions}=await responseData(id);const ordered=[...questions].sort((a:any,b:any)=>a.sort_order-b.sort_order);const rows=[['回答日時',...ordered.map((q:any)=>q.title),'合計スコア','平均スコア'].map(csvEscape).join(',')];for(const r of responses as any[]){rows.push([new Date(r.submitted_at).toLocaleString('ja-JP'),...ordered.map((q:any)=>displayAnswer(r.response_answers.find((a:any)=>a.question_id===q.id),q)),r.total_score??'',r.average_score??''].map(csvEscape).join(','))}return new NextResponse('\uFEFF'+rows.join('\r\n'),{headers:{'content-type':'text/csv; charset=utf-8','content-disposition':`attachment; filename="responses-${id}.csv"`,'cache-control':'no-store'}})}
