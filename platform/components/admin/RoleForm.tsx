'use client';import {useActionState} from 'react';import {updateRoleAction} from '@/app/actions';export function RoleForm({id,role}:{id:string;role:string}){const [state,action,pending]=useActionState(updateRoleAction.bind(null,id),null);return <form action={action} className="row"><select name="role" defaultValue={role}><option value="sales">営業</option><option value="admin">管理者</option></select><button className="btn secondary" disabled={pending}>更新</button>{state?.error&&<span className="error">{state.error}</span>}</form>}
