'use client';import {useActionState} from 'react';import {duplicateSurveyAction} from '@/app/actions';export function DuplicateForm({id,name,slug}:{id:string;name:string;slug:string}){const [state,action,pending]=useActionState(duplicateSurveyAction.bind(null,id),null);return <details><summary>複製</summary><form action={action} className="row" style={{paddingTop:8}}><input name="name" aria-label="複製後の名称" placeholder={`${name} のコピー`} required/><input name="slug" aria-label="複製後のslug" placeholder={`${slug}-copy`} required/><button className="btn secondary" disabled={pending}>{pending?'複製中…':'複製する'}</button>{state?.error&&<span className="error">{state.error}</span>}</form></details>}
