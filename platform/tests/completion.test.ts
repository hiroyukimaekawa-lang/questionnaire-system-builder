import test from 'node:test';import assert from 'node:assert/strict';import {evaluateCompletionRules} from '../lib/completion';import {defaultConfig} from '../lib/survey';
test('単一条件でメッセージとfollow-upを返す',()=>{const result=evaluateCompletionRules({...defaultConfig,completionRules:[{id:'low',logic:'and',conditions:[{questionId:'q1',operator:'lte',value:5}],completionMessage:'改善します',needsFollowUp:true}]},{q1:4});assert.deepEqual(result,{completionMessage:'改善します',needsFollowUp:true,matchedRuleId:'low'})});
test('AND条件はすべて一致した場合だけ成立する',()=>{const config={...defaultConfig,completionRules:[{id:'high',logic:'and' as const,conditions:[{questionId:'q1',operator:'gte' as const,value:8},{questionId:'q2',operator:'gte' as const,value:8}],completionMessage:'ありがとう'}]};assert.equal(evaluateCompletionRules(config,{q1:8,q2:7}).matchedRuleId,null);assert.equal(evaluateCompletionRules(config,{q1:8,q2:9}).matchedRuleId,'high')});
test('OR条件はいずれか一致すれば成立する',()=>{const config={...defaultConfig,completionRules:[{id:'low',logic:'or' as const,conditions:[{questionId:'q1',operator:'lte' as const,value:5},{questionId:'q2',operator:'eq' as const,value:1}],completionMessage:'確認します'}]};assert.equal(evaluateCompletionRules(config,{q1:9,q2:1}).completionMessage,'確認します')});
test('完了条件の結果はGoogle口コミ設定を変更しない',()=>{const config={...defaultConfig,googleReviewMode:'all' as const,googleReviewUrl:'https://example.com',completionRules:[{id:'low',logic:'and' as const,conditions:[{questionId:'q',operator:'lte' as const,value:5}],completionMessage:'low'}]};evaluateCompletionRules(config,{q:1});assert.equal(config.googleReviewMode,'all');assert.equal(config.googleReviewUrl,'https://example.com')});
