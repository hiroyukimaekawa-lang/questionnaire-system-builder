---
name: japanese-web-typography
description: 日本語Webサイトの見出し・本文・CTA・カード・サンクス画面等を、意味・日本語の自然さ・デザイン・レスポンシブ表示を基準に自然な改行へ整えるSkill。新規制作、既存修正、最終Visual QAで必ず使用する。
---

# Japanese Web Typography Skill

## 目的

日本語Webサイトのテキストについて、単純な文字数ではなく「意味」「日本語の自然さ」「デザイン」「レスポンシブ表示」を基準に、自然で読みやすい改行・折り返しへ調整する。

このSkillは以下の3用途で使用する。

1. **新規制作時**：初回実装から自然な改行設計を行う。
2. **既存修正時**：不自然な改行、孤立した助詞、固有名詞・文節・語尾の分断、スマホ表示崩れを検出して修正する。
3. **最終Visual QA時**：コードだけで判断せず、実画面・スクリーンショットを確認し、意味途中の折り返しが残っていないことを確認する。

## questionnaire-system-builder での適用範囲

このリポジトリでは、以下をすべて対象とする。

- `platform/components/survey/SurveyRenderer.tsx`
- `platform/components/survey/ThanksPanel.tsx`
- 公開アンケート画面
- 管理画面内の公開プレビュー
- Hero / H1 / H2 / 質問タイトル / 選択肢 / CTA
- 店舗・医院名
- 匿名案内
- サンクス画面
- Google口コミ案内
- 自由記述の再表示

公開画面と管理画面プレビューは同じRendererを使い、店舗別の場当たり的な改行CSSを追加しない。

---

## 使用トリガー

以下の依頼・状況ではこのSkillを使用する。

- 「日本語の改行を直して」
- 「スマホで見出しが変な位置で折れる」
- 「文字組みを整えて」
- 「見出しの改行位置を自然にして」
- 「助詞だけ次の行になるのを直して」
- 「店舗名や商品名が途中で切れている」
- 「レスポンシブ時の日本語表示を調整して」
- 日本語のアンケート公開画面を新規制作・変更する
- Hero / H1 / H2 / CTA / カードタイトルを実装する
- サンクス画面を実装・変更する
- 最終提出前のVisual QAを行う

---

## 完了条件

次をすべて満たすまで完了扱いにしない。

- 日本語として自然な位置で折り返されている
- 助詞・句読点・記号だけが孤立していない
- 店舗名・ブランド名・商品名などの固有名詞が不自然に分断されていない
- PCだけでなくスマートフォンでも自然に読める
- 文字がコンテナからはみ出していない
- 重要見出しが過剰に4〜5行以上になっていない
- 不要な `<br>` の乱用がない
- 「から」「こと」「たい」「ない」「れる」「ます」「ため」等の途中で不自然に分断されていない
- Heroだけでなく、質問タイトル・選択肢・CTA・サンクス画面・口コミカードを確認している
- 375px / 390px / 430px / Tablet / Desktopで実画面またはスクリーンショットを確認している
- コード上の正しさだけでなく、実画面として自然である
- Visual QAで問題が見つかった場合、修正→再確認を繰り返している

---

# 1. 基本原則

1. **文字数ではなく意味のまとまりで改行する。**
2. 本文は原則としてブラウザの自然改行を利用する。
3. Hero・大型見出し・CTA・サンクス見出しなど、デザイン上の意図が明確な箇所のみ意味単位ラッパーを検討する。
4. 不自然な改行をCSSだけで無理に隠さず、必要なら文章そのものを自然に短くする。
5. PCよりもスマートフォンでの見え方を必ず確認する。
6. 公開アンケートだけでなくサンクス画面まで一続きの体験として確認する。
7. ブラウザ任せの折り返しが日本語として不自然になる場合、その状態を許容しない。

---

# 2. 改行位置の優先順位

改行位置は以下の順で判断する。

1. 意味の大きな区切り
2. 文節の区切り
3. 並列関係
4. コピーとしてのリズム
5. デザイン上のバランス

### 良い例

```text
ご回答
ありがとうございました。
```

```text
地域の皆様に
愛されるクリニックへ
```

```text
よろしければ、
Googleでもご感想をお聞かせください。
```

---

# 3. 禁止する不自然な改行

### 助詞の孤立

NG:

```text
今後より良い診療環境づくりのため
に活用させていただきます。
```

Better:

```text
今後より良い診療環境づくりのために
活用させていただきます。
```

### 語尾・活用の分断

NG:

```text
ご回答ありがとうございまし
た。
```

```text
お聞かせくださ
い。
```

OK:

```text
ご回答
ありがとうございました。
```

```text
Googleでもご感想を
お聞かせください。
```

### 1〜2文字だけの孤立

NG:

```text
特別な時間
を
```

OK:

```text
特別な時間を
あなたに
```

---

# 4. 助詞・短語の孤立防止

次の語を単独で新しい行の先頭へ置かないようにする。

- は
- が
- を
- に
- の
- と
- へ
- で
- も
- や
- から
- まで
- より

特にデザインコピーでは、次の語の途中・直前直後で不自然に分断しない。

- から
- こと
- たい
- ない
- れる / られる
- です / ます
- という
- ため
- よう

`ありがとうございまし / た。` のような活用語尾の分断はQA不合格とする。

---

# 5. 日本語の禁則処理

以下の文字を新しい行の先頭に置かない。

```text
、 。 ， ． ！ ？ : ; ） ］ 】 》 〉 」 』
```

以下の開始記号を行末に残さない。

```text
（ ［ 【 《 〈 「 『
```

ブラウザ標準の日本語禁則処理を壊す `word-break: break-all` は、日本語本文・見出しでは使用しない。

---

# 6. 固有名詞の分断禁止

以下は可能な限り同一の意味単位として保持する。

- 店舗名
- 医院名
- 会社名
- ブランド名
- 人名
- 商品名
- 地名
- URL
- 電話番号
- 金額
- 日付
- 数値＋単位
- 英語ブランド名

短い固有名詞には限定的に `white-space: nowrap` を使用してよいが、小さい画面ではみ出さないことを必ず確認する。

---

# 7. アンケートのデザインコピー

以下は通常本文ではなく「デザインコピー」として扱う。

- 店舗・医院名
- Heroタイトル
- Heroサブタイトル
- 質問タイトル
- CTA
- サンクス画面H1
- Google口コミ案内
- コメントボックス見出し

ブラウザ任せで意味途中に折れる場合は、文章・幅・font-size・letter-spacing・line-height・意味単位ラッパーの順で調整する。

---

# 8. 意味単位の保持

短い意味のまとまりは必要に応じて `span` で保持してよい。

```tsx
<h1 className="thanks-title">
  <span className="keep-phrase">ご回答</span>
  <span className="keep-phrase">ありがとうございました。</span>
</h1>
```

```css
.keep-phrase {
  display: inline-block;
  white-space: nowrap;
}
```

長文全体を `nowrap` にしない。

---

# 9. Hero・H1のルール

- 原則2〜3行以内
- モバイルで4〜5行以上になる場合はコピーやfont-sizeを見直す
- 助詞だけの行を作らない
- 重要語・活用語尾を途中で分断しない
- 1行へ無理に押し込むことを目的にfont-sizeを極端に小さくしない
- `text-wrap: balance` のみで合格判定しない

サンクス画面の固定見出しなど、短く意味単位が明確な文字列は、意味単位spanで折り返し候補を限定してよい。

---

# 10. H2・質問タイトル

- できるだけ短くする
- 長い場合は意味単位で2行程度に収める
- 1〜2文字だけ次行へ落とさない
- `※必須` と質問本文のレイアウトが相互に不自然な折り返しを作らないよう確認する

---

# 11. 本文

本文では原則として手動 `<br>` を入れない。

代わりに以下で可読性を確保する。

- 一文を長くしすぎない
- 段落を分ける
- テキストコンテナの最大幅を調整する
- line-heightを適切に取る
- ブラウザの自然改行を活用する
- 対応ブラウザでは `word-break: auto-phrase` をprogressive enhancementとして利用する

---

# 12. HTML実装ルール

重要見出しの意図的な改行は、`<br>` の乱用より意味単位ラッパーを優先する。

```tsx
<h1>
  <span className="heading-phrase">ご回答</span>
  <span className="heading-phrase">ありがとうございました。</span>
</h1>
```

固定 `<br>` は最初の解決策にしない。

---

# 13. PCとMobileで改行位置を変える場合

必要な場合のみレスポンシブ専用クラスを使用する。

レスポンシブ用 `<br>` の大量使用は禁止する。

まず以下を試す。

1. コンテナ幅
2. font-size
3. letter-spacing / line-height
4. `text-wrap`
5. `word-break: auto-phrase`
6. 短い意味単位span

---

# 14. 推奨CSS

日本語本文・見出しでは、必要に応じて以下を使用する。

```css
.jp-copy {
  word-break: normal;
  overflow-wrap: break-word;
  line-break: strict;
  text-wrap: pretty;
}

.jp-heading {
  word-break: normal;
  line-break: strict;
  text-wrap: balance;
}

@supports (word-break: auto-phrase) {
  .jp-copy,
  .jp-heading {
    word-break: auto-phrase;
  }
}
```

`text-wrap: balance` / `pretty` だけに依存せず、重要コピーは実画面で確認する。

---

# 15. `white-space: nowrap` の使用

短い固有名詞や意味単位など、分断すると意味が壊れる範囲に限って使用する。

```css
.keep-phrase {
  display: inline-block;
  white-space: nowrap;
}
```

長文全体へ使用しない。

---

# 16. テキスト幅で調整する

不自然な行長を `<br>` で調整する前に、コンテナ幅を検討する。

アンケート公開画面は原則 `max-width: 430px` だが、内部カードのpaddingによって実テキスト幅が狭くなりすぎないように確認する。

特にサンクス画面では、外側padding + カードpadding + 大きなfont-sizeの組み合わせで見出し末尾が1文字だけ落ちやすいため注意する。

---

# 17. 文字数の参考値

絶対ルールではない。

### Hero H1

- PC：1行12〜24文字程度
- Mobile：1行8〜16文字程度

### Question Title

- 8〜24文字程度を目安

### CTA

- 10〜24文字程度

### Thanks H1

- 1〜2行
- 固定文言の場合は意味単位で折り返し候補を限定する

---

# 18. レスポンシブ確認幅

最低限、以下を確認する。

- 375px
- 390px
- 430px
- 768px
- 1024px
- 1440px

スマートフォン中心のアンケートでは375〜430pxを重点確認する。

必要に応じて320pxも追加確認する。

---

# 19. 既存画面の修正手順

1. 対象ページを確認する
2. Header / Hero / H1 / H2 / Question / CTA / Thanks / Review CTA / 店舗名を確認する
3. 不自然な折り返しを一覧化する
4. 原因を分類する
   - コピーが長すぎる
   - 固定 `<br>`
   - コンテナ幅が狭い
   - font-sizeが大きすぎる
   - `word-break` / `overflow-wrap` が不適切
   - 固有名詞保護がない
   - PC専用改行がMobileへ残っている
5. 最小限の変更で修正する
6. 375px / 390px / 430px / Tablet / Desktopで実画面確認する
7. 公開アンケートだけでなくThanks画面も確認する
8. 新たな不自然な折り返しがないか確認する
9. 問題があれば修正→再表示→再確認を繰り返す

---

# 20. 修正の優先順位

1. コピーを意味を変えない範囲で自然に短くする
2. コンテナ幅を調整する
3. font-sizeを調整する
4. letter-spacing / line-heightを調整する
5. CSSの日本語折り返し設定を調整する
6. 固有名詞・短い意味単位に限定して `nowrap` / `inline-block` を適用する
7. 必要箇所だけ意味単位spanを使用する
8. 必要な場合のみ意図的な改行を設定する
9. 最後の手段としてデバイス別 `<br>` を使用する

---

# 21. コピー変更の判断

現在の文章では自然な改行が作れない場合、意味を維持した上でWeb向けに短くしてよい。

ただし以下は勝手に変更しない。

- 店舗・医院名
- 正式キャッチコピー
- 会社名
- 商品名
- 法的表記
- 営業時間・住所・価格等の事実情報

---

# 22. QAチェックリスト

## 日本語

- [ ] 助詞だけが次の行に落ちていない
- [ ] 句読点から行が始まっていない
- [ ] 開始括弧が行末に残っていない
- [ ] 1〜2文字だけ孤立していない
- [ ] 意味の途中で不自然に切れていない
- [ ] 「から」「こと」「たい」「れる」「ます」等の途中で分断されていない
- [ ] `ありがとうございました。` が `ありがとうございまし / た。` のように分断されていない

## 固有名詞

- [ ] 店舗名・医院名が不自然に分断されていない
- [ ] ブランド名が不自然に分断されていない
- [ ] 数字と単位が不自然に分離していない

## デザイン

- [ ] Heroが過剰な行数になっていない
- [ ] 質問タイトルを全件確認した
- [ ] CTAを確認した
- [ ] Thanks画面を確認した
- [ ] Review CTAを確認した
- [ ] 375pxで確認した
- [ ] 390pxで確認した
- [ ] 430pxで確認した
- [ ] Tabletで確認した
- [ ] Desktopで確認した
- [ ] スクリーンショットまたは実画面を目視した
- [ ] 修正後に再度Visual QAを行った
- [ ] テキストがコンテナからはみ出していない

## コード

- [ ] 不要な `<br>` を追加していない
- [ ] `word-break: break-all` を日本語本文へ使用していない
- [ ] `overflow-wrap: anywhere` を見出し・固有名詞へ安易に使用していない
- [ ] nowrapの範囲が長すぎない
- [ ] Desktop専用処理がMobileを壊していない
- [ ] previewと公開画面で異なるTypography実装を増やしていない

---

# 23. AIの最終判断ルール

改行判断は必ず以下の順で行う。

1. 日本語として自然か
2. 意味のまとまりになっているか
3. 固有名詞を壊していないか
4. 助詞・記号が孤立していないか
5. Mobileで読みやすいか
6. PCで読みやすいか
7. デザインとして美しいか
8. HTML/CSSを過度に固定していないか

**文字数だけを根拠に改行しない。**

---

# 24. 実行時の出力

コード編集が可能な環境では、指摘だけで終わらず対象ファイルを修正し、レスポンシブ表示を確認する。

ユーザーが説明を求めていない場合は、内部で問題箇所と理由を確認した上で実装を優先する。

---

# 25. Visual QAループ

コード編集だけで完了判定しない。

```text
実装
↓
公開画面 / Preview / Thanksを表示
↓
375px / 390px / 430px / Tablet / Desktopで確認
↓
Header・Hero・質問・CTA・Thanks・口コミ案内を目視
↓
不自然な改行を検出
↓
修正
↓
再表示
↓
再度スクリーンショットまたは実画面確認
↓
問題がなくなるまで繰り返す
```

以下はHTML/CSS上エラーがなくてもQA不合格。

- `チームか / ら`
- `なれるこ / と`
- `つくりた / い`
- `場所 / を`
- `ありがとうございまし / た。`
- 1〜2文字だけ次行に残る
- Desktopでは自然だがMobileで意味途中に折れる
- Mobileでは自然だがDesktopの狭いカードで意味途中に折れる

**「コードとして正しい」ではなく、「日本語として自然に読める」が合格条件。**

---

# 26. 提出前の最終ゲート

1. `JAPANESE_WEB_TYPOGRAPHY_SKILL.md` を読んだ
2. Header / Hero / H1 / H2を確認した
3. 全質問タイトル・選択肢を確認した
4. CTAを確認した
5. 店舗・医院名を確認した
6. Thanks画面を確認した
7. Google口コミ案内を確認した
8. 375px / 390px / 430pxを確認した
9. Tablet / Desktopを確認した
10. 実画面またはスクリーンショットを目視した
11. 修正後の再確認を行った
12. 日本語として違和感のある改行が残っていない

1つでも未確認なら提出完了としない。

---

## questionnaire-system-builder の固定実装方針

- `SurveyRenderer` と `ThanksPanel` に共通の日本語Typography classを適用する。
- 日本語見出しには `line-break: strict` / `word-break: normal` / `text-wrap: balance` を基本とする。
- 日本語本文には `line-break: strict` / `word-break: normal` / `text-wrap: pretty` を基本とする。
- 対応ブラウザでは `word-break: auto-phrase` をprogressive enhancementとして使用する。
- 短い意味単位だけ `inline-block + nowrap` を許可する。
- サンクス画面の「ご回答ありがとうございました。」は、語尾が分断されない意味単位で保持する。
- `overflow-wrap: anywhere` はURL・自由入力等の緊急折り返しが必要な場所に限定する。
- 公開画面とプレビューのTypographyを共通化し、店舗ごとの専用CSSを増やさない。
